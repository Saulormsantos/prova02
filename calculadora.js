let a = (10);
let b = (1);
let operador = ("multiplicação");

if (operador == "soma"){
    console.log(a+b)
} if (operador == "divisão")
    console.log(a/b);

        if (operador == "multiplicação")
    console.log(a*b);

            if (operador == "subtração"){
    console.log(a-b);
}
